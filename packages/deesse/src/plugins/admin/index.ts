import { Page, SidebarGroup, SidebarMenu, SidebarSubmenu } from "./types";

export const sidebarGroup = (group: SidebarGroup): SidebarGroup => {
  return group;
};

export const sidebarMenu = (menu: SidebarMenu): SidebarMenu => {
  return menu;
};

export const sidebarSubmenu = (submenu: SidebarSubmenu): SidebarSubmenu => {
  return submenu;
};

export const page = (page: Page): Page => {
  return page;
};
