import { Config } from "deesse";
import { Endpoint } from "./types";

export const GET: Endpoint = (config: Config) => (request: Request) => {
  return {} as Response;
};

export const POST: Endpoint = (config: Config) => (request: Request) => {
  return {} as Response;
};

export const PUT: Endpoint = (config: Config) => (request: Request) => {
  return {} as Response;
};

export const DELETE: Endpoint = (config: Config) => (request: Request) => {
  return {} as Response;
};

export const PATCH: Endpoint = (config: Config) => (request: Request) => {
  return {} as Response;
};

export const OPTIONS: Endpoint = (config: Config) => (request: Request) => {
  return {} as Response;
};
